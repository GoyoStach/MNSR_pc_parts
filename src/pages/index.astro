---
import { getCollection } from 'astro:content';
import { CardGame } from '@/components/CardGame';
import '@/styles/global.css';

const pcParts = await getCollection('pc_parts');
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>PC Parts Card Game</title>
	</head>
	<body class="min-h-screen bg-gradient-to-br from-violet-500 via-purple-500 to-indigo-600">
		<header class="text-center py-16 px-8 text-white">
			<h1 class="text-5xl font-bold mb-4">Welcome to PC Parts Guessing Game</h1>
			<p class="text-xl opacity-90">Reveal the hidden cards by entering your guess below</p>
		</header>
		
		<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
			<div class="bg-white/95 backdrop-blur-sm rounded-3xl shadow-2xl p-4 sm:p-6 lg:p-8 -mt-8">
				<CardGame pcParts={pcParts} client:load />
				
				<div class="text-center mt-6 lg:mt-8">
					<div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
						<input 
							type="text" 
							id="gameInput" 
							placeholder="Enter PC part name..."
							class="px-4 py-3 text-base lg:text-lg border-2 border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500 w-full max-w-sm lg:max-w-md"
						/>
						<button 
							id="submitBtn"
							class="px-6 py-3 text-base lg:text-lg bg-violet-600 hover:bg-violet-700 text-white font-semibold rounded-lg transition-all duration-200 hover:scale-105 hover:shadow-lg active:scale-95 w-full sm:w-auto"
						>
							Submit
						</button>
					</div>
				</div>
			</div>
		</main>
	</body>
</html>
