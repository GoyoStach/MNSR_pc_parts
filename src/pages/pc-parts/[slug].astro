---
import { getCollection, type CollectionEntry } from 'astro:content';
import '../../styles/global.css';

export async function getStaticPaths() {
  const pcParts = await getCollection('pc_parts');
  return pcParts.map((part) => ({
    params: { slug: part.slug },
    props: { part },
  }));
}

type Props = {
  part: CollectionEntry<'pc_parts'>;
};

const { part } = Astro.props;
const { Content } = await part.render();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>{part.data.title} - PC Parts Guide</title>
  </head>
  <body class="min-h-screen bg-gradient-to-br from-violet-500 via-purple-500 to-indigo-600">
    <header class="text-center py-12 px-8 text-white">
      <nav class="mb-8">
        <a 
          href="/" 
          class="inline-flex items-center gap-2 px-4 py-2 bg-white/20 backdrop-blur-sm rounded-lg hover:bg-white/30 transition-colors"
        >
          ← Back to Game
        </a>
      </nav>
      <h1 class="text-4xl lg:text-5xl font-bold mb-4">{part.data.title}</h1>
      <p class="text-xl opacity-90">{part.data.description}</p>
    </header>
    
    <main class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 pb-16">
      <div class="bg-white/95 backdrop-blur-sm rounded-3xl shadow-2xl p-6 lg:p-8 -mt-8">
        
        {/* Image placeholder */}
        <div class="w-full h-64 lg:h-80 bg-gray-100 border-2 border-dashed border-gray-300 rounded-xl flex items-center justify-center mb-8">
          <div class="text-center">
            <div class="text-6xl mb-4">🖼️</div>
            <span class="text-gray-500 text-lg">Image placeholder for {part.data.title}</span>
          </div>
        </div>

        {/* Content area for future markdown content */}
        <div class="prose prose-lg max-w-none">
          <Content />
          
          {/* Placeholder content since markdown files are empty */}
          <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mt-8">
            <h3 class="text-lg font-semibold text-blue-800 mb-2">About {part.data.title}</h3>
            <p class="text-blue-700">
              This page will contain detailed information about the {part.data.title}. 
              Content coming soon!
            </p>
            
            <div class="mt-6 grid grid-cols-1 md:grid-cols-2 gap-4">
              <div class="bg-white rounded-lg p-4">
                <h4 class="font-semibold text-gray-800 mb-2">Key Features</h4>
                <ul class="text-gray-600 space-y-1">
                  <li>• Feature 1 coming soon</li>
                  <li>• Feature 2 coming soon</li>
                  <li>• Feature 3 coming soon</li>
                </ul>
              </div>
              
              <div class="bg-white rounded-lg p-4">
                <h4 class="font-semibold text-gray-800 mb-2">Specifications</h4>
                <ul class="text-gray-600 space-y-1">
                  <li>• Spec 1: Coming soon</li>
                  <li>• Spec 2: Coming soon</li>
                  <li>• Spec 3: Coming soon</li>
                </ul>
              </div>
            </div>
          </div>
        </div>
        
        <div class="mt-8 text-center">
          <a 
            href="/" 
            class="inline-flex items-center gap-2 px-6 py-3 bg-violet-600 hover:bg-violet-700 text-white font-semibold rounded-lg transition-all duration-200 hover:scale-105 hover:shadow-lg"
          >
            ← Back to Game
          </a>
        </div>
      </div>
    </main>
  </body>
</html>